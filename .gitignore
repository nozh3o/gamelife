# Резервные копии прогресса — это личные данные, в публичный репозиторий им нельзя
gamelife-backup-*.json

# Служебное
.claude/
Thumbs.db
desktop.ini

# Локальная привязка к проекту Supabase (supabase link) — служебные файлы
# конкретной машины, в том числе строка подключения к боевой базе
supabase/.temp/
